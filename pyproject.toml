[project]
name = "lark-agent-bridge"
version = "0.1.0"
description = "Local Feishu Bot bridge for controlled AI agent tasks."
requires-python = ">=3.11"

[tool.setuptools.package-data]
lark_agent_bridge = ["templates/*.html"]
